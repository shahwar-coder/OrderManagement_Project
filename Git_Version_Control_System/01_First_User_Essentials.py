'''
🚀 Git Basic Workflow — From Local to GitHub

🏁 Step 1 — Initialize Git in Your Project Folder
-----------------------------------------------
git init


🧑‍💻 Step 2 — Configure Git (One-time Setup)
-------------------------------------------
git config --global user.name "Shahwar-coder"
git config --global user.email "naqvishahwar120@gmail.com"


📄 Step 3 — Create or Modify Files in Project
--------------------------------------------
git add -A        # Move all files to staging area
git commit -m "commit message"


🌱 Step 4 — Set Main Branch Name
--------------------------------
git branch -M main


🌍 Step 5 — Connect Local Repo to GitHub
---------------------------------------
git remote add origin <GitHub repo URL>


📤 Step 6 — Push Code to GitHub (First Time)
-------------------------------------------
git push -u origin main


✨ After first push, simply use:
git push


🔹 Quick Summary:

git init                     → Start Git in the folder
git add -A                   → Stage all files
git commit -m "msg"          → Create checkpoint
git branch -M main           → Set default branch to main
git remote add origin URL    → Connect to GitHub
git push -u origin main      → Upload project online

'''
